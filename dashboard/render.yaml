services:
  - type: web
    name: dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app_dashboard:app --workers 2 --worker-class sync --timeout 60
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
      - key: RENDER